#include <iostream>
#include <fstream>

using namespace std;

int main(){
    string line;
    ifstream entrada;
    entrada.open("input.txt");

    while(getline(arquivo,line)) cout << line << endl;

    entrada.close();
    return 0;
}
